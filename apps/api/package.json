{
  "name": "api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc -b",
    "start": "node ./dist/index.js",
    "postinstall": "if [ -d dist ]; then ls -R ./dist; fi"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/jwt": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@repo/zod": "workspace:*",
    "express": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "axios": "^1.11.0",
    "bcrypt": "^6.0.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/express": "^5.0.3",
    "@types/cors": "^2.8.19"
  }
}
